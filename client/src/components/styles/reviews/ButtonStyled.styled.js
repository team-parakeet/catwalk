import styled from 'styled-components';

export const ButtonStyled = styled.button`
  font-size: 12pt;
  background-color: #fff;
  text-align: left;
  border: 0px;
  color: #000000;
  padding: 0px;

  &:hover {
    transform: scale(1.1);
  }
`
